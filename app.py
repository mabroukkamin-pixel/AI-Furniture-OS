import streamlit as st

# إعداد واجهة الهاتف
st.set_page_config(page_title="نظام الإدارة السريع", layout="centered")

st.title("👑 لوحة التحكم السريعة")
st.write("أهلاً بك في نظام التشغيل الخاص بنا. اختر المهمة للبدء فوراً:")

# خيارات سريعة وبسيطة
option = st.radio(
    "ماذا تريد أن تفعل الآن؟",
    ("سجل طلب جديد", "قائمة الأسعار والأرقام", "دليل التشغيل السريع")
)

if option == "سجل طلب جديد":
    st.subheader("إضافة طلب جديد")
    client_name = st.text_input("اسم العميل")
    phone = st.text_input("رقم الهاتف")
    service = st.selectbox("نوع الخدمة", ["دهانات", "تصميمات", "ديكورات", "أخرى"])
    if st.button("حفظ الطلب"):
        st.success(f"تم حفظ طلب العميل {client_name} بنجاح!")

elif option == "قائمة الأسعار والأرقام":
    st.subheader("البيانات المعتمدة")
    st.info("رقم التواصل الرسمي للكويت: 97542202")
    st.write("الأسعار محدثة وفقاً للقائمة الرسمية.")

elif option == "دليل التشغيل السريع":
    st.subheader("كيف تستخدم النظام؟")
    st.write("1. اختر المهمة من القائمة أعلاه.")
    st.write("2. املأ البيانات المطلوبة.")
    st.write("3. اضغط حفظ وسأقوم أنا بالمتابعة فوراً.")

st.divider()
st.write("بواسطة: القائد الأعلى - أمين مبروك")
